{
  "name": "arcana-license-server",
  "version": "1.0.0",
  "description": "License validation API for arcana",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@noble/curves": "^1.8.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.8.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
